/// Credenciais da API do Google Gemini (visão: identificação por foto e
/// quiromancia).
///
/// Copie este arquivo para `gemini_credentials.dart` (que é ignorado pelo
/// git) e preencha com a sua chave do Google AI Studio
/// (https://aistudio.google.com/apikey). Com a chave vazia, o app cai para
/// o modelo de visão do Groq (preview, bem mais fraco).
class GeminiCredentials {
  static const String apiKey = '';
}
